# ===========================================
# TatvaOps Vision - Worker Service Configuration
# ===========================================
#
# This worker service processes AI generation jobs from SQS queues.
# It is extracted from moodboard-main and integrated into the platform.
#
# CRITICAL: This worker contains all AI API keys.
# Never expose these in frontend or public API.
#
# ===========================================

# Service
SERVICE_NAME=tatvaops-vision-worker
NODE_ENV=production

# ===========================================
# DATABASE (Supabase Transaction Pooler recommended)
# ===========================================
# Use Supabase Transaction pooler (port 6543) for more concurrent connections.
# Format: postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@aws-1-REGION.pooler.supabase.com:6543/postgres
# URL-encode the password (e.g. @ → %40). Same URL for backend and worker.
DATABASE_URL=postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres

# ===========================================
# REDIS (AWS ElastiCache)
# ===========================================
REDIS_URL=redis://your-redis-endpoint:6379
REDIS_ENABLED=true

# ===========================================
# AWS CREDENTIALS
# ===========================================
# If running on EC2 with IAM role, leave these empty
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1

# ===========================================
# AWS S3 BUCKETS
# ===========================================
S3_BUCKET_FLOORPLANS=tatvaops-vision-production-floorplans
S3_BUCKET_MOODBOARDS=tatvaops-vision-production-moodboards
S3_BUCKET_RENDERS=tatvaops-vision-production-renders
S3_BUCKET_EXPORTS=tatvaops-vision-production-exports

# ===========================================
# AWS SQS QUEUES
# ===========================================
# Get these from Terraform output: terraform output sqs_queue_urls
SQS_QUEUE_FLOORPLAN_ANALYSIS=https://sqs.ap-south-1.amazonaws.com/ACCOUNT_ID/tatvaops-vision-production-floorplan-analysis
SQS_QUEUE_MOODBOARD_GENERATION=https://sqs.ap-south-1.amazonaws.com/ACCOUNT_ID/tatvaops-vision-production-moodboard-generation
SQS_QUEUE_INTERIOR_VIEW_GENERATION=https://sqs.ap-south-1.amazonaws.com/ACCOUNT_ID/tatvaops-vision-production-interior-view-generation
SQS_QUEUE_COMPONENT_UPDATE=https://sqs.ap-south-1.amazonaws.com/ACCOUNT_ID/tatvaops-vision-production-component-update
SQS_QUEUE_NOTIFICATION=https://sqs.ap-south-1.amazonaws.com/ACCOUNT_ID/tatvaops-vision-production-notification

# SQS Settings
SQS_VISIBILITY_TIMEOUT=300
SQS_MAX_RECEIVE_COUNT=3

# ===========================================
# GOOGLE GEMINI AI
# ===========================================
# API Key - required
GEMINI_API_KEY=AIzaSyCzgs9zeMtsnwWoJnZeKspW704MYWdaA3g

# Image generation timeout (ms). Default 180000 (3 min). Increase if you see "Image generation timeout".
# GEMINI_IMAGE_TIMEOUT_MS=180000

# Image generation model (for moodboard generation)
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview
# Fallback image model if primary fails (e.g. timeout, API error)
GEMINI_IMAGE_MODEL_FALLBACK=gemini-3.1-flash-image-preview

# Text analysis model (for image analysis and intent extraction)
GEMINI_TEXT_MODEL=gemini-2.5-flash

# API Versions
GEMINI_TEXT_API_VERSION=v1
GEMINI_IMAGE_API_VERSION=v1beta

# ===========================================
# RUNWAY (Image-to-Video for Walkthroughs)
# ===========================================
# Optional. When set, room walkthrough videos use Runway Gen-4 Turbo (image-to-video)
# instead of Google Veo 3.1. Get key from https://docs.dev.runwayml.com/
# RUNWAY_API_KEY=your_runway_api_key

# ===========================================
# AWS SES (Email Notifications)
# ===========================================
SES_FROM_EMAIL=noreply@tatvaops.com
SES_REPLY_TO_EMAIL=support@tatvaops.com

# ===========================================
# MSG91 (WhatsApp Notifications)
# ===========================================
MSG91_AUTH_KEY=
MSG91_SENDER_ID=
MSG91_TEMPLATE_ID=

# ===========================================
# AWS CLOUDWATCH (Logging)
# ===========================================
CLOUDWATCH_ENABLED=true
CLOUDWATCH_LOG_GROUP=/tatvaops/vision/worker
CLOUDWATCH_LOG_STREAM=

# ===========================================
# WORKER SETTINGS
# ===========================================
# Number of concurrent jobs (default in code is 1 to avoid DB pool exhaustion).
# Set to 2+ only if your database allows multiple connections (e.g. pooled URL).
WORKER_CONCURRENCY=1

# Poll interval in seconds (long polling)
WORKER_POLL_INTERVAL=20

# Maximum time in seconds to process a single job
# Image generation typically takes 30-90 seconds
WORKER_MAX_PROCESSING_TIME=300

